import { defineStyle } from "@chakra-ui/react";

const textTheme = defineStyle({
  baseStyle: {
    fontSize: "18px",
    color: "black",
    fontWeight: "600",
  },

  variants: {

  },

  defaultProps: {
    variant: "default",
  },
});

export default textTheme;
